<Window x:Class="FinanceManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinanceManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DockPanel Background="#0d1217">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <ToolBar Grid.Column="1" Grid.ColumnSpan="2" Height="35" Background="#0d1217">
                <Label Style="{StaticResource LabelStyle}">Current limit is</Label>
                <Label x:Name="LimitLabel" Style="{StaticResource LabelStyle}"></Label>
                <Label Style="{StaticResource LabelStyle}">$</Label>
                <Button Style="{StaticResource ButtonlStyle}" Click="ChangeLimit_Click">Change limit</Button>
            </ToolBar>

            <Button Grid.Column="3" Style="{StaticResource ButtonlStyle}" HorizontalAlignment="Left" Click="AddCategory_Click">Add category</Button>

            <ListBox x:Name="CategoriesListBox"  Grid.Row="1" Grid.Column="3" Margin="5" Background="#0d1217" MouseDoubleClick="ShowExpenses_DoubleClick"></ListBox>
        
        </Grid>

    </DockPanel>
</Window>
